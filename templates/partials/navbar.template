<nav class="navbar navbar-expand-md navbar-top navbar-dark" data-priority-nav>
  <div class="container-fluid priority-nav-container">
    <a class="navbar-brand" href="/" aria-label="Yubico Developers home"><img src="/logo.png" alt="Yubico Developers"></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarToggler" aria-controls="navbarToggler" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="navbar-collapse collapse" id="navbarToggler">
      <ul class="navbar-nav me-auto mb-2 mb-md-0" data-priority-items>
        {% for sect in nav -%}
          {%- if sect.hidden -%}
          {%- elif sect.active -%}
            <li class="nav-item" data-priority-item><a class="nav-link active fw-bold" aria-current="page" href="{{ sect.url }}">{{ sect.name }}</a></li>
          {%- else -%}
            <li class="nav-item" data-priority-item><a class="nav-link fw-bold" href="{{ sect.url }}">{{ sect.name }}</a></li>
          {%- endif -%}
        {%- endfor %}
        <li class="nav-item dropdown" data-priority-more hidden>
          <button class="nav-link dropdown-toggle fw-bold" type="button" aria-expanded="false" aria-controls="priorityNavOverflow">More</button>
          <ul class="dropdown-menu dropdown-menu-end priority-nav-overflow" id="priorityNavOverflow" data-priority-overflow hidden></ul>
        </li>
      </ul>
    </div><!--/.navbar-collapse -->

    <!-- Google Custom Search Engine -->
    <div id="search-box" class="navbar-form navbar-right priority-nav-search" role="search" aria-label="Site search">
        <script type="text/plain" data-cookiecategory="functional">
        (function() {
          var cx = '4d44ab063517e6a2e';
          var gcse = document.createElement('script');
          gcse.type = 'text/javascript';
          gcse.async = true;
          gcse.src = 'https://www.google.com/cse/cse.js?cx=' + cx;
          var s = document.getElementsByTagName('script')[0];
          s.parentNode.insertBefore(gcse, s);
        })();
        </script>
        <gcse:searchbox-only></gcse:searchbox-only>
    </div>
    <!-- End Google Custom Search Engine -->
  </div>
</nav>
